{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "whatsapp-remote",
  "description": "Minimal grant for remote WhatsApp Web origins embedded in account windows: window controls used by the injected titlebar/fullscreen polyfill, plus emitting/listening to the fixed zw:// events the Rust side handles. Tauri only applies these permissions to a remote-URL webview when the capability declares a matching `remote` block — this is the ONLY capability that should declare one for whatsapp.com.",
  "windows": ["main", "account-*"],
  "remote": {
    "urls": ["https://web.whatsapp.com", "https://*.whatsapp.com"]
  },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-set-fullscreen",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
